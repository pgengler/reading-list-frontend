<ul class="list">
{{#each book in books}}
	<li>
		<div>
			{{book.title}}
			<br>
			{{#if book.authors}}
				<span class="authors">
				{{#each author in book.authors}}
					{{author.name}}
				{{/each}}
				</span>
			{{/if}}
		</div>
	</li>
{{/each}}
</ul>
